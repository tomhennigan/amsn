#! /bin/sh

if ps x -o cmd |grep banshee | grep -v grep |wc -l >& /dev/null ; then
	banshee --hide-field --query-title
	banshee --hide-field --query-artist
	banshee --hide-field --query-uri
else
	echo 0
fi

exit 0
